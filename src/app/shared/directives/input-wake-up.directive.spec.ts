import { InputWakeUpDirective } from './input-wake-up.directive';

describe('InputWakeUpDirective', () => {
  it('should create an instance', () => {
    const directive = new InputWakeUpDirective();
    expect(directive).toBeTruthy();
  });
});
