import { UserEventAnchorDirective } from './user-event-anchor.directive';

describe('UserEventAnchorDirective', () => {
  it('should create an instance', () => {
    const directive = new UserEventAnchorDirective();
    expect(directive).toBeTruthy();
  });
});
