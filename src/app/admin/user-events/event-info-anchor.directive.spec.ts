import { EventInfoAnchorDirective } from './event-info-anchor.directive';

describe('EventInfoAnchorDirective', () => {
  it('should create an instance', () => {
    const directive = new EventInfoAnchorDirective();
    expect(directive).toBeTruthy();
  });
});
